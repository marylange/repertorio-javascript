/*
    O método assert.fail() fornece a capacidade de gerar uma 
    falha com base em testes que não são encapsulados pelos outros métodos. 
*/

// Ex:

{
    if (actual.IndexOf(expected) < 0)
        Assert.Fail(message);
}