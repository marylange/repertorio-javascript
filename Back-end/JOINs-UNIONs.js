/*
    Sintaxe do Join

    SELECT t1.coluna, t2.coluna //o que eu quero pegar
    FROM tabela1 AS t1
    INNER JOIN tabela2 AS t2
    ON t1.coluna_em_comum = t2.coluna_em_comum;

    PQ USAR ALIAS(AS)?

    O alias é usado para apelidar qualquer parte da sua query.
    Isso ajuda a identificar qualquer coluna que estamos nos referindo.



    Sintaxe LEFT JOIN e LAST JOIN
*/