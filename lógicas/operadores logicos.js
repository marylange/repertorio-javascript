//Os operadores logicos são
// Eles podem ser usados APENAS!!!!!!!!!!!!!!!!!! com valores BOOLEANOS!!!!!!!!!!!!!!!
// && Retorna verdadeiro apenas se as duas condições forem verdadeiras
// || Retorna verdadeiro se qualquer uma das duas condições forem verdadeiras
// ! Reverte/nega o booleano usado com o !

// Exemplos de operações que geram booleanos

const nome = "Mary";
const lista = ["Mary", "Cleiton", "Cacau"];

if (nome === "Cleiton") { }

if (nome !== "Cleiton") { }

if (nomeQueNaoExiste === undefined) { }

//
if (lista.includes("Josefina")) { }

// Apenas entra no if se as duas condições retornarem true
if (nome !== "Cleiton" && lista.includes(nome)) { }


